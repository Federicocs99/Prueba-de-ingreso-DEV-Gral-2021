El Proyecto fue manejado mediante GitHub y como editor de texto Visual Studio Code Por lo que explicare como funciona en este, lo primero seria copiar el repositorio y abrirlo con Visual Studio Code donde ser verán 4 carpetas con mi solución a los problemas propuestos para correr el problema 1 se necesitarán escribir en la terminal los comandos cd '.\Problema 1 Modelado Clases\' y después dotnet run Este programa tiene la estructuración de clases pedidas y además un programa con consola para verificar su funcionamiento.
 Para el problema 2 moviéndose a la carpeta del problema y ejecutando en la consola dotnet run además se tiene que abrir una página web con la dirección localhost:5001 en caso de tenerlo por defecto. se tiene un sitio web básico que tiene dos selectores para el origen y el destino, un seleccionador de fecha y el botón cta esta pagina web no tiene ninguna funcionalidad.
Problema 3 moviéndose a la carpeta del problema 3 ejecutando en la consola dotnet run se podrá ver una pequeña demostración en consola de la conexión que se hizo con el api en la que se envía un json de con la forma especificada y nos devuelve todos vuelos encontrados con la estructura del problema 1.
Ahora quise integrar estos 3 problemas porque considero que es más cercano a lo que nos pedían para esto la carpeta Integración que se ejecutara al ingresar en la terminal el comando consola dotnet run y abrir el sitio web en localhost:5001 donde se verán dos campos donde ingresar la cuidad de origen y destino en IATA ( a estos campos les faltan muchas validaciones como la cuidad existe entre otras) y un selector de fecha del vuelo( no se alcanzó conectar con el bakend) y al oprimir el botón se buscan mediante la api y los vuelos disponibles para la fecha y estos se guardan en una lista de la clase Flight del problema 1 y se muestran en consola me hubiera gusta hacer que se redirigieran a una nueva pagina y mostrar los resultados.
Con respecto al problema 4 no se alcanzó a realizar, pero para conectarme y guardar información en la base de datos hubiera usado Entity Framework para guardar las clases en él.
